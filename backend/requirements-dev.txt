# Development / test / reproducibility dependencies (not required at runtime).
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==7.4.4
pytest-asyncio==0.23.5
fpdf2==2.8.6          # SYNOPSIS.md -> SYNOPSIS.pdf generation (tools/)
